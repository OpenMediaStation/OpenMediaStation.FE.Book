name: open_media_station_book
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  open_media_station_base: ^0.1.3
    # path: ../open_media_station_base
  epub_view: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  msix: ^3.7.0

flutter:
  uses-material-design: true
  assets:
    - assets/myapp.png

msix_config:
  display_name: Open Media Station - Book
  publisher_display_name: LNA-DEV
  publisher: CN=7B47E609-6C44-44B4-9DFA-CF58A4704989
  identity_name: 54515LNA-DEV.OpenMediaStation-Book
  msix_version: 0.0.1.0
  store: true
  logo_path: ./AppIcon.png
  # capabilities: internetClient, location, microphone, webcam


